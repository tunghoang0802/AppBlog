@model List<Category>
@if (Model != null)
{
    <div class="widget-content">
        <ul class="list">
            @foreach (var category in Model)
            {
                <li><a href="/@category.Alias">@category.CatName</a></li>
            }
        </ul>
    </div>
}

